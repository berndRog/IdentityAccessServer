@using System.Globalization
@using BankingBlazorSsr.Api.Dtos
<div class="card shadow-sm w-100">
  <div class="card-body">
    <h5 class="card-title">Konto</h5>

    @if (AccountDto != null)
    {
      <table class="table table-sm table-striped">
        <thead>
        <tr>
          <th class="fw-normal">Kontonummer</th>
          <th class="fw-normal">Kontostand</th>
        </tr>
        </thead>
        <tbody>
        <tr>
          <td>@AccountDto.IbanString</td>
          <td>@AccountDto.BalanceDecimal.ToString("C", CultureInfo.CurrentCulture)</td>
        </tr>
        </tbody>
      </table>
    }
    else
    {
      <p>Keine Kontodaten verfügbar.</p>
    }
  </div>
</div>

@code {
  [Parameter] public AccountDto? AccountDto { get; set; }
}
